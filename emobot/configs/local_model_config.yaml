# 本地模型配置
model:
  type: "local"
  name: "microsoft/DialoGPT-medium"  # 或其他适合的本地模型
  max_length: 1000
  temperature: 0.7

# 工具配置
tools:
  web_search: true
  email: true
  todo_list: true

# 服务器配置
server:
  host: "127.0.0.1"
  port: 8080 